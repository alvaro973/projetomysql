<main>
        <h2>home</h2>

    {{#each books}}
    <div>
        <h3>Titulo: {{thais.title}}</h3>
        <p>QUantidade de paginas: {{thais.pageqty}}</p>
          <a href="/book/{{this.id}}">ver mais</a>  
    </div>
{{/each}}
</main>